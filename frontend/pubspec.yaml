name: qraulaprueba
description: "AulaQR - Sistema de información de aulas mediante códigos QR"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  
  # Iconos y UI
  cupertino_icons: ^1.0.8
  
  # Networking
  http: ^1.6.0
  flutter_dotenv: ^6.0.0
  
  # Almacenamiento local
  shared_preferences: ^2.2.2
  
  # Escáner QR
  qr_code_scanner: ^1.0.1
  mobile_scanner: ^6.0.2
  
  # Generador de QR
  qr_flutter: ^4.1.0
  
  # Utilidades
  universal_platform: ^1.1.0
  provider: ^6.1.1
  path_provider: ^2.1.1
  image_gallery_saver: ^2.0.3
  permission_handler: ^11.3.1
  share_plus: ^7.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  assets:
    - .env
  # 🔴 LAS FUENTES ESTÁN COMENTADAS - Las agregas solo si tienes los archivos
  # assets/images/ tampoco existe, lo dejamos fuera por ahora
  
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: assets/fonts/Roboto-Regular.ttf
  #       - asset: assets/fonts/Roboto-Bold.ttf
  #         weight: 700
  #       - asset: assets/fonts/Roboto-Italic.ttf
  #         style: italic